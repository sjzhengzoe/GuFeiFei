const precss = require('precss');const autoprefixer = require('autoprefixer');const flexBugsFixes = require('postcss-flexbugs-fixes');// const discard = require('postcss-discard-duplicates');module.exports = {  plugins: [    // discard,    flexBugsFixes,    // https://browserl.ist    precss,    autoprefixer({      remove: false, // 因为之前的样式，一堆都是只写了 -webkit-xxxx 这种情况...所以如果改这里会导致那些代码不生效    }),  ],};